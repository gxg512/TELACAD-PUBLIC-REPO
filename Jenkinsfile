pipeline {
    agent any

    stages {
        stage('my message') {
            steps {
                script {
                    echo 'this is a message from the public git repo'
                }
            }
        }
    }
}
